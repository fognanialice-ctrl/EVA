import WordReveal from "@/components/ui/WordReveal";

export default function Statement() {
  return (
    <WordReveal
      text="L'abbondanza non si divide. / Si moltiplica."
      goldWords={2}
    />
  );
}
